import State from "../State";

export default function popupReducer(state = State.popupManager, action: { type: string, payload?: any }) {
    switch (action.type) {
      default:
        return state
    }
  }